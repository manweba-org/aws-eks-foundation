## Summary

## Checklist

- [ ] `make fmt` / `make validate` (and ideally `make test`) pass locally
- [ ] CI checks green: `fmt / init / validate`, `lint / scan / policy`
- [ ] No secrets, credentials, real account IDs, or `.terraform/` / state files
- [ ] Docs / examples updated if behavior or variables changed
- [ ] Intentional scanner skips documented in existing ignore configs

## Test plan

- [ ]
